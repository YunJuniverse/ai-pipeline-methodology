# 캡슐 트리아지 판정 초안 — 2026-09-18 (METH-148)

> 수거 6회차. `collect --apply` 신규 10건(원장 68→78). 판정은 사람 확정 필요 — 본 문서는 AI 조사·초안.
> 전 건 상류 대조: observe 파서·thinktank 본문 · catalog `_README` 승급 규칙 · 지침 21·23·24·25·26·27 · C-002·P-005·P-007.
> ai-icons 도구 매트릭스 2건은 **외부 사실**이라 1차 출처를 직접 확인했다(OpenAI Help Center · BFL 라이선스 원문 · BFL 문서 · Hugging Face 모델 카드). 확인 결과는 §A.

## 판정 요약

| # | 캡슐 | target | 판정 초안 | 분배 초안 |
|---|------|--------|-----------|-----------|
| 1 | ai-icons__2026-09-16_design-image-tool-matrix-2026q3 | guide-26 | **유효(사실 대조 동반·일부 Class C)** | 지침 26 v2 — 확인된 사실만 · 라이선스 문장은 사람 승인 |
| 2 | ai-icons__2026-09-16_design-video-tool-matrix-2026q3 | guide-27 | **유효(사실 대조 동반)** | 지침 27 v2 + 25 §1 Sora 날짜 정밀화 + QA 3항 |
| 3 | cafe24-renewal__2026-09-17_friction-phase-split | tool/observe | **유효(판단 동반)** | friction 선택 필드 `phase`(닫힌 5값) |
| 4 | cafe24-renewal__2026-09-17_retro-friction-aggregate | tool/thinktank | **유효** | thinktank `--path`·비용 합계·월별·재발 비중·상위 사례 |
| 5 | cafe24-renewal__2026-09-17_incident-to-blocking-tool | catalog | **유효(target 재배치)** | 지침 23 §1-6 신설 「문장 규칙 → 차단 도구 승급 기준」 |
| 6 | cafe24-renewal__2026-09-17_merge-is-deploy-ci-path | catalog | **유효 → active C-003** | 4개 날짜 재현 + 5회차 캡슐 동일 원인 |
| 7 | cafe24-renewal__2026-09-17_hosted-platform-observability-bootstrap | skeleton/… | **유효(스켈레톤 보류)** | `_pending/P-009` 우산 항목 — P-005·P-007·C-003 묶음 |
| 8 | cafe24-renewal__2026-09-17_third-party-integration-registry | catalog | **부분 이미 반영 + 병합** | 새 부분(레지스트리 필드)만 P-009 ③ 으로 |
| 9 | cafe24-renewal__2026-09-17_interaction-spec-before-build | catalog | **유효(target 재배치)** | 지침 21 §2 wireframe-spec 경계 1행 + 23 §2-7 스캔 기법 1줄 |
| 10 | icons__2026-09-14_probe-tool-capability-before-claiming | guides | **대부분 이미 반영 + 1줄** | 지침 24 §4 인스턴스 1줄 |

집계: **유효 9 · 부분 이미 반영 1 · 만료 0.**
분배: 지침 5갈래(21·23·24·25·26·27 중 6개 문서) · 도구 2(observe·thinktank) · catalog **C-003 승급 1** + pending 1.

---

## A. 도구 매트릭스 2건 — 1차 출처 확인 결과 (#1·#2)

ai-icons 리서치(`40_dev/snapshots/2026-09-16_design-ai-skills-landscape-research/`)는 URL 을 달았고 §8 에 「원문 미확인」 목록까지 둔 정직한 문서다. 다만 상류 지침 문장과 어긋나는 곳이 있어 핵심 4개를 직접 열었다.

| 주장 | 상류 현재 문장 | 1차 출처 확인 | 판정 |
|---|---|---|---|
| Sora API 2026-09-24 종료 | 25 §1 「Sora(2026-04 서비스 종료)」 | OpenAI Help Center: **웹·앱 2026-04-26 종료, API 2026-09-24 종료** | **양쪽 다 맞다.** 상류는 앱 날짜만 적었다 → API 날짜 추가(6일 뒤 종료) |
| FLUX.2 다중 참조 10장 | (없음) | BFL 문서: **API 8장 · 플레이그라운드 10장 · [dev] 권장 최대 6장 · [klein] 4장** | 「10장」은 플레이그라운드 수치 — 경로별로 적는다 |
| FLUX.2 [dev] 비상업 · [klein] 4B Apache-2.0 | 26 §1 「dev 웨이트는 *출력만* 상업 OK」 | BFL [dev] Non-Commercial License v2.0 · HF 모델 카드 | **상류 문장이 위험하게 불완전하다** — 아래 |
| GPT Image 2 참조 16장 | 26 §1 (참조 수 없음) | 리서치 출처가 `getimg.ai …rumours-leaks…` 블로그(3자·루머) | **지침에 넣지 않는다** — 공식 출처 확인 전까지 |

**FLUX [dev] 라이선스 — 상류 지침 26 의 문장이 틀린 방향으로 요약돼 있다.** 라이선스 원문은 두 조항이 같이 있다.
- §2(d) Outputs: 출력은 상업 목적 포함 어떤 목적에도 쓸 수 있다(경쟁 모델 학습 금지).
- 그러나 §2(a)·(b)·§4(a): 모델 **자체의 사용**은 Non-Commercial Purpose 에만 허용되고, 정의상 «수익 활동» «최종 사용자에게 영향을 주는 사용»은 비상업이 아니다.

즉 «출력은 상업 OK» 만 적으면 **회사가 업무 산출물을 만들려고 [dev] 를 돌리는 것 자체가 허용되는 것처럼 읽힌다.** 캡슐의 「dev 비상업」이 운영 규칙으로는 더 정확하다. [klein] 4B 는 Apache-2.0(상업 가능), [klein] 9B 는 비상업 라이선스로 확인됐다.
→ 이 문장 교체는 **법무·컴플라이언스 안내의 변경**이라 CLAUDE.md §3 **Class C 후보**다. 사람 승인 전에는 반영하지 않는다(판단 ②). 이 초안은 법률 자문이 아니다 — 라이선스 원문을 옮긴 것이다.

나머지 주장(Nano Banana Pro 참조 14장 — 출처 openrouter · 단가표 · 영상 모델 스펙)은 리서치의 출처를 따르되 **반영 시 공식 문서로 재확인**한다(지침 24 §4). 지침 25 §1 은 이미 「도구 지형은 분기 단위로 변한다 — 분기 재검증」을 규정한다 — 이 캡슐 2건이 바로 그 분기 재검증이다.

**#2 에서 도구 사실이 아닌 부분**은 판단 없이 유효하다: ① 상용 비디오 모델에 알파채널 공식 지원이 없으므로 스티커·루프는 생성 후 배경 제거 조립을 전제 ② 플랫폼 규격 기계 검사(예: LINE 320×270 APNG 1MB) ③ 기계 지표(봉합 LPIPS·유사도·플리커)를 지침 27 §5 QA 체크리스트에.

## B. 관찰 로그·회고 도구 (#3·#4)

**상류 실측**: `parse_friction_item` 은 `where|cost|resolution|repeat_of` 4필드. `thinktank` 는 `where` 문자열 빈도와 `repeat_of` 건수만 센다 — **비용(cost_minutes) 합계·월별 추이·재발 비용 비중·상위 사례가 없고, 대상이 상류 repo 하나로 고정**(`observation_files()` 가 `METHODOLOGY_ROOT` 만 본다). 상류에만 friction 이 든 관찰로그가 179건 있다.

- **#4 유효.** cafe24 는 842건에서 마찰 204건·4,684분·재발 21% 를 임시 파서로 뽑았다. 같은 척도를 표준 명령으로 만들면 repo 간 비교가 된다. 분배: `thinktank --path <repo>` + 비용 합계 · 월별 · 재발(repeat_of) 비용 비중 · 비용 상위 사례 N건. 스키마 변경 없이 된다.
- **#3 유효 — 판단 동반.** `phase`(diagnose·fix·deploy·verify·communicate)가 있으면 «진단 비용이 수정 비용을 넘는다»(cafe24 회고의 핵심 발견)를 키워드 추정 없이 잰다. **스키마를 *넓히는* 변경**이라 기존 179건은 그대로 유효하다 — 지침 23 §4-4 의 소급 재검증은 *좁힐 때*의 규칙이라 해당 없다(수거 직후 checkpoint 에 «재검증 필요»로 적은 것을 정정). 남는 질문은 선택이냐 필수냐다 — 판단 ①.

## C. 문장 규칙 → 차단 도구 승급 기준 (#5, 유효·target 재배치)

catalog 생애주기는 `마찰 → pending → active C → skeleton` 이다. **«지침 문장 → 실행 시 차단 도구» 로 올리는 단계와 그 기준은 어디에도 없다.** 그런데 이 방법론은 그 일을 반복해 왔다: build-guard(METH-122 「SOP 문서만으로는 못 막았다」) · wrap 구조 검증(METH-143) · 훅 읽기 전용(METH-146) · ship 공유 체크아웃 가드(METH-147). 캡슐은 그 관행의 기준을 준다 — **동일 원인 재발 2회 또는 운영 화면 노출 1회면 문장이 아니라 도구로.** cafe24 근거: 재발 표시 마찰 42건 1,005분(21%)이 «문장 규칙만 있던 구간»이었다.
**분배**: 지침 23 §1-6 신설(검증 규범 — §1-3 «금지형 가드는 negative case 로 실효 증명»과 한 쌍) + 규칙 조항에 대응 도구 경로를 함께 적는다 + catalog `_README` §3 생애주기에 한 줄. target 은 catalog 였으나 규칙이라 지침으로.

## D. 배포 자격증명 — 머지=배포 CI 경로 (#6, 유효 → active C-003)

같은 원인(배포 자격증명이 로컬 파일에만 있음)이 **4개 날짜에 재현**(08-22·09-02·09-14·09-17) — catalog `_README` 의 active 기준(seen_in N≥2)을 이미 넘는다. 5회차 캡슐 `boot-deploy-access-preflight` 도 같은 원인이었고 상류는 그때 **증상 경고**(boot `required_local_files`)만 넣었다. 이 캡슐은 **근본 해법**이다: 시크릿은 CI 저장소에만, `main 머지 = 배포`, 가드 6종(변경분만 · N개 초과 차단 · 삭제 미반영 · 부분 실패 비0 종료 · PR 트리거에서 시크릿 비노출 · 수동 재실행·dry_run), 로컬 즉시 배포와 공존.
부수 발견 «업로드 스크립트가 실패해도 exit 0» 은 지침 23 §1-1(0건 처리=실패)·§1-4(exit code 판정)의 인스턴스.
**분배**: `C-003_deploy-credentials-local-only` active. **다운스트림에서 채택하는 순간 `.github/workflows/` 변경이라 Class B**(land 가 자동 머지를 거부한다) — C-003 에 «채택 PR 에 결정 근거·영향 범위·롤백·시크릿 리스크 4항 필수»를 명시한다. 상류는 패턴만 두고 워크플로 파일은 배포하지 않는다(배포 대상이 repo 마다 다르다).

## E. 호스팅 플랫폼 계열 (#7·#8)

cafe24-renewal 에서 같은 계열이 넷째다 — P-005(캐시 적대 표시 게이트) · P-007(배포 대상 드리프트) · #7 · #8. 네 개 모두 METH-142 발신 규칙의 판별 질문(«다른 플랫폼에서도 참인가»)을 통과한다 — Cafe24 고유 사실이 아니라 호스팅 플랫폼 일반의 규율이다.

- **#7 유효 — 우산 항목.** «기능 착수 전 첫 주에 관측 인프라부터» 5항(① 렌더 파이프라인이 무엇을 합치고 지우는지 ② 캐시 계층·반영 시간 실측 ③ 서드파티 점유·쓰기 인벤토리 ④ 자격증명 없는 배포 경로 ⑤ 렌더 기반 검증 스크립트)이 기존 조각을 한 순서로 묶는다: ②=지침 23 §4-5·P-005 · ③=#8·P-007 · ④=C-003 · ⑤=지침 23 §2-6·C-002. 근거가 크다 — 마찰 4,684분 중 약 49% 가 «무엇이 실렸는지 확정»하는 비용이었다. 플랫폼 선정 기준에 «관측·통제 가능성»을 넣자는 제안도 함께.
  스켈레톤 `hosted-platform-customization` 은 **만들 수 없다** — `skeletons/_README`: pending 은 bake 대상이 아니다. 분배: `_pending/P-009_hosted-platform-observability-first` 로 등재하고 P-005·P-007 을 하위로 링크. 승급 조건 = **두 번째 호스팅 플랫폼 프로젝트**에서 재현.
- **#8 부분 이미 반영.** 비공식 API 폴백·롤백 스위치 = 지침 24 §4(METH-147) · «부재 판정 전 렌더 방식 확인» = 지침 23 §2-6 · 서버 파일 직접 수정 이력 = P-007. **새로운 것은 레지스트리 자체**(점유 슬롯·서버 파일 수정 여부·반영 지연·렌더 방식·비활성 방법·롤백의 필드 목록)다. 분배: P-009 ③ 항목으로 흡수.

## F. 인터랙션 수치 스펙 (#9, 유효·target 재배치)

지침 21 §2 의 wireframe-spec 은 «화면별 5블록·3-state» — **스크롤 고정·등장 타이밍·전환 연출의 수치 스펙이 빠져 있다.** 정적 디자인 도구에 없는 것이라 명세에도 없고, 그래서 구현 중에 값을 더듬는다(cafe24 20건 638분: 고정 레이어 튐 75분·핀 지연 부작용 60분). 5px 스크롤 스캔·40ms 겹침 스캔 도입 후 수렴했다.
**분배**: 지침 21 §2 에 wireframe-spec 경계 한 줄(«정적 도구에 없는 인터랙션은 트리거 위치·고정/해제 좌표·지속시간·겹침 허용을 수치로») + 지침 23 §2-7(애니메이션 실측) 에 «스크롤 N px·프레임 단위 기계 스캔» 기법 한 줄 + «값 조정으로 못 없애는 구조적 부작용은 되돌리고 선택지를 수치와 함께 제시».

## G. 외부 에이전트 CLI 능력 (#10, 대부분 이미 반영)

지침 24 §4 가 «확인하지 않은 플랫폼 동작을 단정해 안내하지 않는다»(METH-131)와 «블랙박스는 원본을 읽어 확정»(METH-147)을 이미 규정한다. 새로운 것은 **방법**뿐이다 — 스크래치 폴더에서 헤드리스로 한 번 실행해 본다. 오판의 비용도 적을 만하다: 이미지 생성 불가로 단정해 **사용자에게 API 키(비밀)를 요구**했다. 분배: 24 §4 에 인스턴스 1줄.

---

## 사람 판단이 필요한 지점 (3)

1. **friction `phase` — 선택 필드 vs 필수 필드.** 선택이면 기록 부담이 없지만 비어 있는 로그가 많아져 비교가 약해진다. 필수면 11 repo 전부 매 마찰마다 한 칸이 늘고 형식 오류로 wrap 이 설 수 있다. **권고: 선택 + thinktank 가 phase 기입률을 함께 보고**(비어 있음이 숨지 않게). 소급 채우기는 하지 않는다.
2. **지침 26 FLUX 라이선스 문장 교체 — Class C.** 권고 문안: «FLUX.2 [dev]·[klein 9B] = 비상업 라이선스 — 업무·수익 활동에서 **모델을 구동하는 것 자체**가 라이선스 밖이다(출력 조항과 별개). 상업 산출물은 [klein 4B](Apache-2.0)·BFL API·상업 라이선스 경로로.» 법률 자문이 아니라 원문 요약이며, 확정 전 법무 확인을 권한다. 이 판단은 기존 문장을 **뒤집는** 것이라 AI 가 정하지 않는다.
3. **지침 26·27 에 단가·스펙 수치를 계속 둘지.** 이번처럼 분기마다 바뀌는 수치를 지침에 두면 매 분기 11 repo 전파가 필요하고, 틀린 수치가 «지침»의 권위를 얻는다. **권고: 지침에는 역할·원칙·라이선스 등급만, 단가·참조 장수·모델 스펙은 날짜 스냅샷 표로 옮기고 지침은 그 스냅샷을 가리킨다**(지침 25 §1 «모델명이 아니라 역할로»의 문서 버전).

비채택·보류: GPT Image 2 참조 16장(루머 출처) · 스켈레톤 `hosted-platform-customization`(pending 은 bake 불가).

## 처리 순서(확정 후)

1. 판단 무관분 먼저: C-003 · P-009 · 지침 23 §1-6·§2-7 · 21 §2 · 24 §4 · 25 §1 Sora 날짜 · 27 QA 3항 · thinktank 확장.
2. 판단 ① → observe `phase` + thinktank 기입률.
3. 판단 ③ → 지침 26·27 구조(수치 분리 여부) 결정 후 매트릭스 갱신. 판단 ② 는 그 안에서 라이선스 행만 따로 승인.
4. `_inbox` 정리(원장 78 유지) → 전파 → 훅 재설치.
