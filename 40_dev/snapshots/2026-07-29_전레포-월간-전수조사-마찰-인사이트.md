# 전 레포 월간 전수조사 — 병목·반복·결과물 문제·인사이트 (2026-06-29 ~ 07-29)

> 날짜 산출물(스냅샷) — 라이브 아님. 2026-07-29, 병렬 조사 에이전트 11기가 각 repo의
> 관찰로그(friction)·HANDOFF·checkpoint·TODO·git log·스냅샷을 읽기 전용 전수 조사한 결과의 교차 집계.
> 목적: 캡슐 루프(METH-117) 가동 이전에 쌓인 미환류 인사이트의 소급 수거. 승급은 사람 게이트(백서 §8-2).
> 커버리지: 이 머신 11개 repo. grooman(타 호스트)은 조사 불가 — 커버리지 밖.

---

## 0. 총괄 통계

| repo | 관찰로그 | friction | 비용(분) | 커밋 | 특기 |
|---|---:|---:|---:|---:|---|
| ai-icons | 284 | 87 | 1,521 | 268 | 최대 활동. repeat_of 유의미 기재 56% |
| gamblescan | 90 | 53 | 1,330 | 162 | 반복 마찰이 비용의 40% |
| talmo-com | 73 | 35 | 590 | 84 | 포착률 48%로 최고 |
| lifeManager | 55 | 32 | 496 | 113 | 자동화 검증 불가 클러스터 |
| tshome | 60 | 26 | 453 | 118 | 단일 최고 비용(CI 불일치 60분+4일) |
| icons-invest | 184 | 26 | 449 | 192 | IR 덱 트랙 — 지침 22 기환류 다수 |
| icons | 98 | 18 | 259 | 194 | 포착률 18% — checkpoint와 괴리 |
| icons-marketing | 30 | 13 | 208 | 51 | 데이터 무결성 클러스터 |
| insta-toon | 10 | 7 | 55 | 20 | 스택-PR 13일 미도달 |
| invest-ops | 10 | 5 | 50 | 21 | 민감정보 저장 범위 미합의(High) |
| cafe24-renewal | 112 | **0** | — | 71 | **friction 스키마 100% 미기입** — 병목이 산문에 매몰 |
| **계** | **1,006** | **302** | **≈5,411 (90h+)** | **1,294** | cafe24는 미기록이라 실비용 과소집계 |

---

## 1. 교차-repo 반복 패턴 — 승급 후보 (N≥2, 비용·재발순)

### P1. 스택-PR / 머지 방향·도달 미검증 — **6개 repo, 실사고 다수** ★최우선
- 증거: methodology(METH-096~098 main 미반영), insta-toon(PR 3단이 중간 브랜치로 머지 — **13일간 main에 코드 없음, TODO는 Done**), icons-marketing(MKT-013 누락→#11 복구), lifeManager(#25 고아→프로덕션 미반영, "GitHub은 base 삭제 시만 리타깃" 오안내), icons(머지 타이밍 사고 4회 — 구버전 프로덕션 배포 1회), gamblescan(stacked-pr×db push, main 직접 푸시→revert)
- 제안: ① "스택-PR 금지·main 직행 단일 PR" 을 개인 메모리가 아닌 **전 repo 지침으로 승급** ② **Done 전이 조건에 `git merge-base --is-ancestor <커밋> origin/main` 검증**(wrap 또는 ship에 기계 검사) ③ ship의 원격 대조(METH-115)는 이식 완료 — 남은 건 "머지≠main 도달≠배포" 3단 구분
- lifeManager 관찰로그가 이미 규칙 문장으로 정리해 둠: "앞 PR이 머지된 뒤 main에서 새로 분기한다"

### P2. 관찰로그 스키마·CLI 결함 — **9개 repo 전부** ★방법론 자체 결함
- 증거: `authored_by/domain/flow_used/stack_used` 전량 CLI 기본값(unknown·meta·ad-hoc — 9개 repo 합계 900+건 동일), `prompt_patterns` 전량 "l1 observation capture/success:true/rounds:1"(성공률 지표가 구조적으로 항상 100%), **friction 파일당 1건(F-001) 캡** 의심(lifeManager·ai-icons·gamblescan 실측), `repeat_of` 포맷 붕괴(icons 7종 변형·tshome/lifeManager 키 중복 오염·talmo 자연어 — 기계 집계 불가), cafe24는 friction 0/112(선택 필드는 아무도 안 채움)
- 제안: observe CLI 개선 — ① 실행 환경에서 agent/tool/host_os 자동 채움 ② 기본값 그대로면 lint "미기입" 처리 ③ `repeat_of`를 enum(null | 관찰 session_id)으로 강제+validate ④ 다건 friction 지원 ⑤ prompt_patterns는 미기재 시 빈 배열(METH-118과 통합 검토)
- **이 결함이 thinktank·캡슐 루프의 입력을 말리고 있음** — 최우선급

### P3. 라이브 파일 규칙 미작동 (비대·신선도·Blocked 미사용) — **7개 repo**
- 증거: cafe24(checkpoint 345줄/342KB·TODO 358KB·HANDOFF "최근 5건"에 11건), gamblescan(TODO 761줄·"PR 대기" 42회 전부 stale·HANDOFF/checkpoint/wrap-state 3파일 시점 상이), icons(TODO 한 줄 6,628자), icons-invest(한 줄 12,000자·Blocked 미사용 — 최장 19일 병목이 Ready 체크박스에), talmo(Blocked 0건인데 실 병목 3종), insta-toon(HANDOFF 13일 stale·Open Issues 공백), invest-ops(wrap 5일 미실행 무경고)
- 제안: ① wrap 사이즈 경고(METH-101)를 **경고→fail-closed 승급 또는 자동 아카이브 회전**(ai-icons가 회전 5회로 유일하게 유지) ② **외부 게이트(대표·결제·자격증명) 대기 = Blocked 강제 이동** 규칙 ③ HANDOFF 신선도(최종 갱신일 vs 최근 커밋일) 검사 ④ wrap 미실행 N일 경과 감지

### P4. "성공처럼 보이는 실패"(무음 실패) 프로토콜 — **6개 repo**
- 증거: gamblescan(`.catch(()=>[])`가 수익 표면 소등 은폐·SVG `--` 무음 파괴·head SIGPIPE exit0·검사기 0파일 통과), talmo(Vercel 한도 무음 스킵·zsh 루프·빈 스크린샷·title join 조용한 유실), lifeManager(Lightning CSS 미참조 변수 제거 35분·vercel CLI 부분 업로드), ai-icons(silent-key-mismatch 40분·빌드 실패해도 구버전으로 조용히 성공), icons-invest(리드백 없는 빌드), icons(HMR 캐시 404)
- 제안: 검증 규범 지침에 ① "0건 처리·빈 산출물 = 실패로 간주(sanity assert)" ② "검사 못 함 ≠ 깨끗함 구분(대상 0개면 exit 1)" ③ 금지형 가드는 **negative case 주입으로 실효 증명** ④ "빌드 통과 ≠ 산출물 정상 — 산출물 grep/리드백 필수"

### P5. 정본(SoT)·전제 확인 게이트 — **5개 repo, 고비용**
- 증거: ai-icons(하이웍스=정본 단정→13티켓 덮어쓴 뒤 전면 반전, 195분 클러스터), gamblescan(사용자가 그은 경계를 검증 없이 2회 거절 90분·조사문서 진단 오신뢰 2회 80분), invest-ops(당사자 관계 미확인→딜 메모 전면 재계산 30분), talmo(시안=사양 vs 참고 미합의 25분), tshome(요청 표면 진단을 코드로 반증해야 — "IR 관리 어렵다"의 실제 갭은 딴 곳)
- 제안: 착수 게이트 지침 — ① 과거 사실 기록물은 "무엇이 정본인가" 사용자 확인 선행 ② 조사 스냅샷 진단은 착수 시점 코드로 재확인 ③ 시안·요청의 해석 계약(정확 구현 vs 참고)을 첫 질문으로 ④ 사용자가 그은 경계는 거절 전 원문 검증

### P6. dev 캐시·빌드 충돌 (.next 계열) — **4개 repo, 최다 반복**
- 증거: ai-icons(**dev 중 build 7회** — SOP 박제에도 재발), tshome(.next stale CSS 4회), icons(dev 중 build+HMR 캐시), icons-marketing(Turbopack stale 3회·68분)
- 제안: **규칙이 아니라 강제** — build 래퍼가 dev 서버 감지 시 중단(또는 preview_stop 선행)하는 스크립트를 스캐폴드에 포함. "7회 반복"이 문서 규칙의 한계를 실증

### P7. 외부 소스 차단·크롤 페이스 — **6개 repo**
- 증거: gamblescan(크롤 페널티 4단 승급 190분 — 허용량 1,451→311 누적 감소·18h 휴식 무효·VPN 무효·타 소스 접근성까지 파괴, `SOP_scraping-pace.md` 박제됨), insta-toon(WebFetch 403 2회→Firecrawl/Semantic Scholar 폴백), talmo(Unsplash 차단), icons-marketing(인스타 차단), invest-ops(시세 fetch 차단), icons-invest(WebFetch 403)
- 제안: ① gamblescan의 스크래핑 페이스 SOP를 **상류 표준 SOP로 승급** ② 리서치 flow에 폴백 사다리(WebFetch→Firecrawl→브라우저→API 탐색 우선) 명문화 ③ "폴백 사용 시 산출물에 정밀도 한계 표기"

### P8. CI-로컬 환경 불일치 — **3개 repo**
- 증거: tshome(npm 검증 vs Vercel pnpm — 60분+**프로덕션 4일 정지**), gamblescan(zod 유령 의존성 배포 중단·lockfile drift 2회→가드 편입), insta-toon(.env.example↔로더 부재)
- 제안: ① "CI가 쓰는 패키지 매니저로 검증" 규칙 + `packageManager` 핀·락파일 단일화를 스캐폴드 기본 ② lockfile sync 가드(gamblescan `check-lockfile-sync.mjs`)를 재사용 자산으로 ③ "런북 작성 시 절차를 실제로 밟는다 = 통합 테스트"

### P9. 빈 상태 검증 = 미검증 / 검증은 내용·가시성 기준 — **5개 repo**
- 증거: icons-marketing(데이터 0건이라 Bars 파손 은폐), icons(DOM 존재만 확인→클리핑 버그 배포·URL만 믿어 404 컷 채택), gamblescan(RSC 스트리밍이라 HTML grep 거짓 음성·200이 아니라 기대 요소 렌더 확인), talmo(빈 화면 캡처·카드 크기 검수로 브랜드 라벨 못 봄→380px 기준), lifeManager(scrollWidth 측정으로만 잡히는 결함)
- 제안: UI·산출물 검증 지침 — 비어있지 않은 데이터로, 최대 표시 크기로, 존재가 아닌 내용·가시성(elementFromPoint/스크린샷/렌더 텍스트) 어서션으로

### P10. 병행 세션 충돌 (git add -A·티켓 선점·라이브 파일) — **3개 repo + 구조 비판**
- 증거: ai-icons(티켓 충돌 5회 — add -A가 남의 미커밋 흡수), icons-invest(sync 커밋 WIP 혼입 — 기존 Open Issue), gamblescan friction 원문 — **"방법론이 매 작업마다 라이브 파일 3종 갱신을 의무화해서 병렬 PR은 구조적으로 100% 충돌한다"**(처리량을 1로 고정)
- 제안: ① ship 타깃 스테이징 기본화(add -A 폐지) ② 티켓 번호 원자 할당(max+1 재조회) ③ **라이브 파일 갱신 시점 재설계 검토**(머지 시점 분리 또는 append-only) — 방법론 구조에 대한 정면 비판이라 RFC감

### P11. AI 생성·샘플 데이터가 사실인 척 — **4개 repo**
- 증거: tshome(지어낸 채용 복지 8건이 **2개월 라이브 게시**, 잔여 미검증 다수 — Open Issue), icons-marketing(리서치 가짜 통계 4건·`[샘플]` 접두+제거 스크립트 원칙 보유), talmo(자기충족 가짜 판매량→익일 재설계), invest-ops(잠정 수치 ≈ 표기)
- 제안: "사실 주장(수치·정책·연혁)은 출처 필드 없이 라이브 금지 + 플레이스홀더 명시 마킹" 전 도메인 지침. icons-marketing의 `[샘플]` 규칙이 승급 원형

### P12. 검증 불가 → 사용자 이관 등록부 — **4개 repo**
- 증거: lifeManager(6건 93분 — 드래그·업로드·인증 UI, 우회를 매번 재발명), tshome(Studio 로그인 — "승인받아도 AI가 못 하는 단계"), ai-icons(뷰포트 0×0), cafe24(실브라우저 필요)
- 제안: "검증불가 등록부"(무엇을·왜·대체 검증·확인 요청 대상) 1급 개념화 + "드래그/업로드 UI는 비-포인터 대안 동반 구현" 규칙

---

## 2. 방법론 repo가 직접 회수할 결함 (도구 백로그 후보)

1. **observe CLI 스키마 강제** (P2) — 메타 자동 채움·repeat_of enum·다건 friction·기본값 lint. METH-118(프롬프팅 블록)과 같은 파일을 만지므로 **통합 구현 권장**
2. **wrap/ship에 main 도달 검증** (P1) — Done 전이·배포 게이트에 merge-base 검사
3. **라이브 파일 fail-closed·자동 회전** (P3) — 사이즈 초과 시 아카이브 강제, Blocked 강제 이동 규칙, 신선도 검사
4. **build 가드 스크립트** (P6) — dev 서버 감지 시 차단, 스캐폴드 포함
5. **pre-push 훅 sync 커밋 면제** — 이번 달 3회 재발(별도 friction 체인 기록 완료)
6. **라이브 파일 병렬 충돌 구조** (P10) — RFC 후보(백서 §2 운영 규칙 변경이라 Class C성)

## 3. 지침 신설·보강 후보 (문서 백로그 후보)

- **검증 규범 지침(신설)**: P4 무음 실패 + P9 내용 기준 검증 + 가드 negative case + "결과 동일 ≠ 배선 증명(역방향 테스트)" — 단일 지침으로 묶을 수 있는 응집성
- **착수 게이트 지침(신설 또는 07 보강)**: P5 정본 확인·해석 계약·경계 검증·조사문서 재확인
- **스크래핑 페이스 SOP 승급**: gamblescan `SOP_scraping-pace.md` → 상류 standing (P7)
- **지침 22 보강 15건**: icons-invest 조사가 미환류 갭을 정밀 식별(텍스트본=유일 소스·리드백 필수·차트 글리프 가드·안정 슬라이드 ID·패널 결함 TODO 승격 루프 등) — 별도 캡슐/티켓으로
- **지침 19 보강**: 스키마 변경 프리플라이트(백필·generated·partial unique — lifeManager 52분), 스케일 의존 상수 grep(talmo 4건)
- **환경 프리플라이트 게이트**: 스택 확정 직후 환경 점검(talmo 100분=병목의 17%)

## 4. repo별 요지·미해결 (상세는 각 repo 정본)

- **ai-icons**: 87건/25.4h. 정본 미확인 축 반전이 최대 사고. InProgress 51일(ICONS-019)·Blocked 13일(296). meth_outbox 0건
- **gamblescan**: 53건/22.2h. 근접 사고 다수 차단(빈 페이지 390개·무면허 낙인 등). 실사고: 라이브 이름 8건 덮어쓰기·env 레코드 삭제·배포 중단. HANDOFF 3파일 시점 불일치
- **talmo-com**: 데모 데이터가 확장마다 붕괴(후기 1,815건 재생성 등). "모바일 대응은 되어 있으나 적합하지 않다"
- **tshome**: 프로덕션 4일 정지 + AI 생성 콘텐츠 2개월 게시(I-006 잔여 미검증 다수 — 후속 필요)
- **lifeManager**: 실계정 데이터 2건 시드로 오염(주의 사례)·KPI 오산출(124 vs 5)
- **icons**: PR 머지 타이밍 4회·수동 배포 루틴 15회(자동화 후보)·외부 게이트 레지스터 부재
- **icons-invest**: 패널 치명 지적의 재발 방지 루프 부재(1차 치명 5 중 2건만 해소된 채 2차 진행). IR-9·IR-13(High) 대표 확인 19일 대기
- **icons-marketing**: 원장 append-only 설계 결함(조용한 거짓말 실패 모드) 미해결 — 커넥터 구현 전 upsert 전환 필요
- **insta-toon**: 스택-PR 3단 미도달(**즉시 조치 필요** — main에 코드 없음, TODO Done 허위). 저작권 증거 유실 위험 13일 방치
- **invest-ops**: **조합원 실명·식별번호·금액이 평문으로 원격 repo에** — 저장 범위 대표 합의 미완(가장 민감한 미해결). capsule_policy: restricted 부여 후보
- **cafe24-renewal**: friction 0 기록이라 luppe 굶김. 요구사항 오해석(행/열 혼동)·브레이크포인트 상수 불일치가 재작업 주원인. 관찰 밀도 붕괴(최근 20일 3건)

## 5. 즉시 주의 항목 (repo 세션에서 조치 필요 — 방법론 아님)

1. **insta-toon**: PR 스택 미도달 복구(main에 lettering·provenance·config 부재, TODO Done 허위 상태)
2. **invest-ops**: 민감정보 저장 범위 대표 합의 + capsule_policy: restricted
3. **tshome**: I-006 잔여 미검증 사실 주장(9년1위·+500% 등) 검증 또는 마킹
4. **icons-marketing**: 원장 upsert 전환(커넥터 구현 전)
5. **icons**: 배포 루틴 자동화·외부 게이트 레지스터

## 6. 방법 한계

- cafe24는 friction 미기록이라 산문 역추출 — 비용 정량 불가, 실 총비용은 90h를 상회
- grooman(타 호스트) 미조사
- 비용(cost_minutes)은 AI 자기신고 추정치
- 각 repo 조사는 단일 에이전트 1패스 — 교차 검증 없음
