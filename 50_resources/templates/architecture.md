# 아키텍처 (Architecture) — 템플릿

> 용도: **어떻게** 만들 것인가. 짝 문서 [`prd.md`](prd.md)(무엇). 현재 코드(as-built)에서 출발해 목표·이전 경로를 기술. **경량 문서 모드**의 2번째 축. 지침 `20_guides/18_개발_마스터_플랜_작성_지침.md` 참조.
> 상태: Draft · 최종 수정 YYYY-MM-DD · 본 문서가 코드 디테일과 어긋나면 **코드를 따른다**.

## 1. 설계 원칙
1. (예: 공개 우선 / 돈·재고는 DB에서 / 결제는 웹훅이 확정 / 데이터 격리 / 점진 이전)

## 2. 현재 스택 (as-built)

| 레이어 | 구현 | 위치 |
|---|---|---|
| 프레임워크 | | |
| 데이터 | | |
| 인증 | | |
| CI/CD | | |

## 3. 목표 아키텍처
```
(읽기 / 상태변경 / 외부 결제·웹훅 / 권한 게이트의 흐름을 텍스트 다이어그램으로)
```

## 4. 기술 스택 (목표)

| 영역 | 선택 | 비고 |
|---|---|---|
| | | |

## 5. 데이터 모델 (핵심 엔티티 — 키 컬럼만)

> 상세 스키마·마이그레이션은 `data-model.md`.

## 6. 권한 모델

| 대상군 | 읽기 | 쓰기 |
|---|---|---|
| 공개 카탈로그 | 공개 | staff/admin |
| 사용자 데이터 | 본인 | 본인(+신뢰 경로) |

## 7. 트랜잭션·무결성 (원자 연산)
- (돈·재고·동시성이 걸린 연산은 앱 레벨이 아니라 *DB 신뢰 경로*에서. 멱등·감사 가능.)

## 8. 통합 (인증·결제·외부 서비스)
- **결제 진실원 = 웹훅 + 멱등 처리** (클라이언트 성공 콜백만으로 확정 금지).

## 9. 미디어 / 스토리지

## 10. 검색

## 11. mock → real 이전 경로 (브라운필드)
1. 스키마 → 2. 시드 → 3. 읽기 교체 → 4. 액션 도입 → 5. 결제 연결 → (PRD 출시 단계 순서를 따른다)

## 12. 목표 디렉토리 (증분)
```
(현 구조에서 *추가/격하*되는 부분만 증분 표기)
```

## 13. 규제의 기술적 매핑 (해당 시)

> PRD §7 규제 요구사항을 *기술 구현*으로 1:1 매핑.

| 규제 (요구사항) | 기술 반영 |
|---|---|
| | |

## 14. 미해결 결정
- (단일 vs 멀티 / 임계 넘는 시점의 교체 등 — 결정 시점과 함께)

**원칙**: 현재 코드가 출발점이다 — 재설계가 아니라 *as-built → 목표 → 이전 경로*로 쓴다. 비가역 아키텍처 결정은 ADR로 분리하고 여기엔 요약만 둔다.
