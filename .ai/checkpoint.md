# 세션 체크포인트
> 세션 서사 바통. 누적 상태는 `HANDOFF.md`.

## 방금 한 것 (2026-09-01 · METH-138 → 139 → 140)
**`land` 의 `plan` 오탐에서 시작해, 같은 파일에 걸린 하류 수정 두 건을 상류로 정리했다.**

- **METH-138**(`0a667c94`) — `plan` 경계 `[./_-]`→`[./]`. 스캐너 첫 테스트 커버리지 신설.
- **METH-139**(`f3fc2864`) — 병렬 세션의 독립 수정(icons#667)과 **상보적**임을 전수 실측으로 확인하고 합집합 채택. icons 2502 경로: 옛 패턴 **824건**(레포 1/3) → 채택안 **2건**(전부 진짜 checkout). 내 단수 `plan` 허용이 오탐이었음을 실측이 뒤집었다.
- **METH-140** — icons#668(캡슐 `origin_repo` 가 워크트리명으로 갈라져 상류 중복 수거)을 상류로 역주입. `_repo_name` 을 `git rev-parse --git-common-dir` 기준으로. METH-137 에서 경고만 남기고 원인 미해소였던 `gamblescan-p0-pr__...` 의 근본 원인.

## 배운 것 (되풀이 방지)
1. **`60_tools/methodology.py` 는 sync 로 내려오는 파일** — 하류가 로컬에서 고치면 다음 sync 에 덮여 버그가 되돌아온다. 하류 수정을 발견하면 **상류 반영 여부를 반드시 함께 확인**한다. 이번에 #667·#668 둘 다 그 상태였다.
2. **오탐도 안전을 깎는다** — 레포 1/3이 Class B 면 사람은 스캐너를 읽지 않고 `--no-ci-check` 로 간다. fail-closed 를 이유로 방치할 근거가 되지 못한다. 근거 숫자(824)를 `test_bare_plan_would_be_catastrophic` 에 박제했다.

## 다음 구체 행동
- METH-140 PR land → icons 재동기화(현재 icons PR #669 가 #668 과 충돌 중 — 상류에 #668 이 들어가면 해소된다).
- 나머지 10 repo 전파(`sync-all`)는 사용자 확인 후.

## 막힌 것
- 없음.

## 환경
- `~/methodology` · 브랜치 `fix/capsule-repo-name-worktree`(base=main `f3fc2864`).
